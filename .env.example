# Local development / deployment reference
LATEX_RENDERER_URL=https://latex-devices.onrender.com
# Set this only if LATEX_RENDERER_TOKEN is configured on the renderer service.
# LATEX_RENDERER_TOKEN=replace-with-your-secret-token
